class Word {
    constructor(
        id,
        categoryId,
        word,
        imageUrl

    ) {
        this.id = id;
        this.categoryId = categoryId;
        this.word = word;
        this.imageUrl = imageUrl;
    }
}

export default Word;